INSERT INTO
  incident.incidents (
    incident_id,
    incident_type,
  sub_type,
    create_time,
    clear_time,
    event_source,
    max_lanes_affected,
    max_priority,
    max_impact,
    status,
    source_name,
    begin_milepoint,
    end_milepoint,
    begin_latitude,
    begin_longitude,
    end_latitude,
    end_longitude,
    route_name,
    route_direction,
    city,
    county,
    recovery_begin_time,
    last_updated,
    max_vehicle_count,
    vehicles_make,
    vehicles_model,
    vehicles_color,
    vehicles_tow_company,
    responders_id,
    responders_action,
    responders_timestamp
  )
SELECT
  incident_id,
  incident_type,
  subtype as sub_type,
  create_time,
  clear_time,
  event_source,
  max_lanes_affected,
  max_priority,
  status,
  source_name,
  begin_milepoint,
  end_milepoint,
  begin_latitude,
  begin_longitude,
  end_latitude,
  end_longitude,
  route_name,
  route_direction,
  city,
  county,
  recovery_begin_time,
  last_updated,
  max_vehicle_count,
  vehicles_make,
  vehicles_model,
  vehicles_color,
  vehicles_tow_company,
  responders_id,
  responders_action,
  responders_timestamp
FROM
  public.temp_incidents_uploads
ON
  conflict do nothing
returning *